# Gradle
.gradle/
build/
.kotlin/
local.properties

# IDE
.idea/
*.iml
captures/
.cxx/

# Signing material never enters the repo
*.jks
*.keystore
*.p12
# ...except the throwaway self-signed certificate for localhost that the fetcher tests serve with.
# It signs nothing, is trusted by nothing, and the tests cannot reach a server without it.
!app/src/test/resources/localhost.p12

# OS
.DS_Store
